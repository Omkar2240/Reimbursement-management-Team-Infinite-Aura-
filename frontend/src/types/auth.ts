export type TSignupFormValues = {
    name: string;
    email: string;
    password: string;
    role: string;
    country: string;
    currency: string;
}

export type TLoginFormValues = {
    email: string;
    password: string;
    role: string;
}