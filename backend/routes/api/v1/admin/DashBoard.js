const express = require('express');

const router = express.Router({ mergeParams: true });
const DashBoardControl = require('../../../../controllers/api/v1/admin/DashBoard');


module.exports = router;
